diff --git a/packages/opencode/src/session/message-v2.ts b/packages/opencode/src/session/message-v2.ts
index 68157f5f4d..626aa5dab6 100644
--- a/packages/opencode/src/session/message-v2.ts
+++ b/packages/opencode/src/session/message-v2.ts
@@ -1192,6 +1192,15 @@ export function fromError(
         },
         { cause: e },
       ).toObject()
+    case e instanceof Error && /certificate/i.test(e.message):
+      return new APIError(
+        {
+          message: e.message,
+          isRetryable: true,
+          metadata: { message: e.message },
+        },
+        { cause: e },
+      ).toObject()
     case e instanceof Error:
       return new NamedError.Unknown({ message: errorMessage(e) }, { cause: e }).toObject()
     default:
diff --git a/packages/opencode/src/session/retry.ts b/packages/opencode/src/session/retry.ts
index 463bc27a95..04b027786d 100644
--- a/packages/opencode/src/session/retry.ts
+++ b/packages/opencode/src/session/retry.ts
@@ -25,6 +25,7 @@ export type Retryable = {
 export const RETRY_INITIAL_DELAY = 2000
 export const RETRY_BACKOFF_FACTOR = 2
 export const RETRY_MAX_DELAY_NO_HEADERS = 30_000 // 30 seconds
+export const RETRY_MAX_DELAY_OVERLOAD = 11_000
 export const RETRY_MAX_DELAY = 2_147_483_647 // max 32-bit signed integer for setTimeout
 
 function cap(ms: number) {
@@ -57,7 +58,11 @@ export function delay(attempt: number, error?: MessageV2.APIError) {
         }
       }
 
-      return cap(RETRY_INITIAL_DELAY * Math.pow(RETRY_BACKOFF_FACTOR, attempt - 1))
+      const effectiveAttempt = ((attempt - 1) % 36) + 1
+      return Math.min(
+        (Math.floor((effectiveAttempt - 1) / 3) + 1) * 1_000,
+        RETRY_MAX_DELAY_OVERLOAD,
+      )
     }
   }
 
