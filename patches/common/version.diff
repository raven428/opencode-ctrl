diff --git a/packages/opencode/src/installation/index.ts b/packages/opencode/src/installation/index.ts
index 47278bd56..39db98308 100644
--- a/packages/opencode/src/installation/index.ts
+++ b/packages/opencode/src/installation/index.ts
@@ -189,7 +189,10 @@ export namespace Installation {
     await $`${process.execPath} --version`.nothrow().quiet().text()
   }
 
-  export const VERSION = typeof OPENCODE_VERSION === "string" ? OPENCODE_VERSION : "local"
+  export const VERSION =
+    (typeof globalThis.OPENCODE_VERSION === "string" && globalThis.OPENCODE_VERSION) ||
+    (typeof process.env.OPENCODE_VERSION === "string" && process.env.OPENCODE_VERSION) ||
+    "local"
   export const CHANNEL = typeof OPENCODE_CHANNEL === "string" ? OPENCODE_CHANNEL : "local"
   export const USER_AGENT = `opencode/${CHANNEL}/${VERSION}/${Flag.OPENCODE_CLIENT}`
 
